package com.onlinecourse.app.repositories;

/*import java.util.Optional;

import org.springframework.data.mongodb.repository.MongoRepository;
import org.springframework.stereotype.Repository;

import com.onlinecourse.app.entities.ERole;
import com.onlinecourse.app.entities.Role;
import com.onlinecourse.app.entities.User;
@Repository
public class RoleRepository extends MongoRepository<Role, String> {
	//finder method
	Optional<Role> findByName(ERole name);
}*/
